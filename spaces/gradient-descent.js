// Градиентный спуск: шарики-оптимизаторы скатываются по поверхности ошибки к минимуму.
const bump = (x, y, cx, cy, s) => Math.exp(-((x - cx) ** 2 + (y - cy) ** 2) / s);

export default {
  meta: {
    id: 'gradient-descent',
    title: 'Градиентный спуск по поверхности ошибки',
    category: 'training',
    order: 10,
    level: 1,
    fidelity: 'real',
    summary: 'Шарик — веса модели, рельеф — ошибка. Посмотрите, как градиентный спуск, момент и Adam ищут минимум, застревают в ямах и «улетают» при слишком большом шаге.',
    uses: 'Так обучается почти любая нейросеть: от распознавания лиц до GPT. Выбор оптимизатора и скорости обучения — ежедневная работа ML-инженера.',
    tags: ['обучение', 'градиентный спуск', 'оптимизаторы', 'Adam', 'learning rate'],
    preview: { surface: true },
  },

  type: 'surface',
  surfaces: [
    {
      id: 'bowl', name: 'Чаша (простая задача)', lr: 0.1, start: [-2.6, 2.2], range: [-3, 3, -3, 3],
      f: (x, y) => x * x + 3 * y * y,
      formula: 'w₁² + 3·w₂²',
      note: 'Один минимум, склоны разной крутизны. Попробуйте η = 0.3: шарик начнёт «прыгать» между стенками и может разойтись.',
    },
    {
      id: 'rosenbrock', name: 'Овраг (функция Розенброка)', lr: 0.01, start: [-1.6, 2.4], range: [-2, 2, -1, 3], log: true,
      f: (x, y) => (1 - x) ** 2 + 10 * (y - x * x) ** 2,
      formula: '(1 − w₁)² + 10·(w₂ − w₁²)²',
      note: 'Узкий изогнутый овраг: шарик быстро падает на дно, а потом медленно ползёт вдоль него. Метод с моментом «разгоняется» и доходит до минимума гораздо быстрее. Adam делает шаги примерно размера η независимо от крутизны — при η = 0.01 он осторожен; попробуйте η = 0.1.',
    },
    {
      id: 'multi', name: 'Несколько минимумов', lr: 0.05, start: [-2.6, 2.4], range: [-3, 3, -3, 3],
      f: (x, y) => 0.08 * (x * x + y * y) - 1.2 * bump(x, y, 1.4, -0.8, 1.2) - 0.7 * bump(x, y, -1.5, 1.4, 0.6) - 0.5 * bump(x, y, -1, -1.8, 0.5) + 1.3,
      formula: 'чаша + три «ямы» разной глубины',
      note: 'Градиентный спуск скатывается в ближайшую яму и застревает в локальном минимуме. Кликните по разным местам поверхности — результат зависит от старта.',
    },
    {
      id: 'saddle', name: 'Седло', lr: 0.05, start: [2.4, 0.02], range: [-2.5, 2.5, -2.5, 2.5],
      f: (x, y) => x * x - y * y + 0.25 * y ** 4 + 1.5,
      formula: 'w₁² − w₂² + 0.25·w₂⁴',
      note: 'В центре — седловая точка: градиент почти ноль, но это не минимум. Спуск долго «топчется» рядом с ней, прежде чем скатиться в одну из ложбин.',
    },
  ],

  intro: `<p>Обучение нейросети — это поиск весов с наименьшей ошибкой. Здесь у «модели» всего два веса, поэтому ошибку можно нарисовать
    как рельеф. На каждом шаге считается <b>градиент</b> — направление самого быстрого роста ошибки — и веса сдвигаются в обратную сторону
    на величину <b>η</b> (скорость обучения).</p>`,

  sections: [
    {
      title: 'Что попробовать',
      html: `<ul class="steps">
          <li>Включите все три оптимизатора и запустите — кто первым доберётся до минимума в «овраге»?</li>
          <li>На «Чаше» увеличьте η до 0.3–0.5: шаг становится слишком большим, и ошибка растёт вместо того, чтобы падать.</li>
          <li>На «Нескольких минимумах» кликните в разные точки: градиентный спуск всегда скатывается в ближайшую яму.</li>
        </ul>
        <p class="muted small">У настоящих сетей миллионы весов, и «рельеф» нельзя нарисовать, но устроен процесс точно так же.</p>`,
    },
  ],
};
